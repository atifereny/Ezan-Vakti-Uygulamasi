import 'package:flutter/material.dart';

const kPrimary    = Color(0xFF7A5C2E); // koyu amber kahve
const kSurface    = Color(0xFFFDF8EE); // açık krem (kart arka planı)
const kScaffoldBg = Color(0xFFF5EDD6); // ılık bej (zemin)
const kOnSurface  = Color(0xFF2C1A08); // koyu kahve (ana metin)
const kMuted      = Color(0xFF8B7355); // soluk kahve (ikincil metin)
const kOutline    = Color(0xFFDCC898); // altın bej (çizgi / kenarlık)
